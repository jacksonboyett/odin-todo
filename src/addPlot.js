export default function addPlot(plotObject) {
	if (plotObject.area === 'Jungle Beach') {
	document.q
	} else {
		console.log('NOOOOO')
	}
}

// The way I have it now, when you go to a new tab, everything erases and repopulates
// when you go back to that tab. 