export default class PlotClass {
	constructor(title, area) {
		this.title = title;
		this.area = area;
	}
}


// Open up plot creator -> output values into a function that constructs those 
// into an object -> put those objects into one object